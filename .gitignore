# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
dist/
build/

# Models (절대 커밋 금지)
*.gguf
*.safetensors
*.onnx
assets/models/

# 오프라인 번들 산출물 (bundle_deps.sh가 생성, 각 빌드 머신에서 재생성)
assets/wheels/
assets/npm_cache/

# 사내 문서 샘플 (PII 위험 — tests/fixtures/hwpx의 합성본만 저장소에 올린다)
assets/hwpx_samples/

# Upstream은 참고용 — clone되지만 git에는 안 올림
upstream/

# Upstream 런타임 파일들이 프로젝트 루트로 흘러들어올 때 무시
avatars/
backgrounds/
characters/
live2d-models/
model_dict.json
web_tool/
cache/
chat_history/

# macOS 실행 번들·런처 (사용자 환경별, 저장소에 안 올림)
새싹이.app/
새싹이.command

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db
desktop.ini

# Logs
*.log
logs/

# Local data
data/
cache/
*.db
*.sqlite
*.sqlite3

# Claude Code 로컬 설정 (팀 공유 안 함)
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Node (향후 UI 빌드 시)
node_modules/
dist-ui/

# Environment
.env
.env.local

# E2E 테스트 실행 산출물
tests/e2e/artifacts/
web/dist/
web/dist-electron/
web/electron-dist/
